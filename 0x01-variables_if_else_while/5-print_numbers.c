#include <stdio.h>

/**
 * main - entry point
 * Description - Printing single digit numbers of base 10.
 * Return: 0 always (Success)
 */
int main(void)
{
	int i;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n)
	}
	printf("\n");
	return (0);
}
